# Progress

Current phase: 6

## Phase 1: Scaffold + data model — audited-pass
Notes: Next.js+TS+Tailwind scaffold; client.config.ts branding read by placeholder page; Prisma schema (Lead, TriageResult, FollowUpDraft, ActionLog) migrated to SQLite via `@prisma/adapter-better-sqlite3` (Prisma 7 requires driver adapters, no plain `url`); repo layer (`lib/repo/leads.ts`, `lib/repo/actionLog.ts`) built TDD with vitest, tests pass; seed script (`prisma/seed.ts`) inserts a lead + action log and reads it back.
Audit: 2026-07-07 — PASS. Verified live: `npx next dev -p 3401` booted, page rendered "Summit HVAC Co." from client.config.ts (HTTP 200); grep confirmed no hardcoded shop name/color outside client.config.ts. `npx prisma migrate status` shows schema up to date, 1 migration (20260707090155_init) containing all 4 CREATE TABLE statements (Lead, TriageResult, FollowUpDraft, ActionLog). `npm test` (vitest): 2/2 tests passed (leads.test.ts, actionLog.test.ts). `npx prisma db seed` inserted and read back lead "Maria Lopez". No Prisma calls found in app/ outside generated client code. Deps logged in decision log below (rule 3 satisfied). .env.example documents DATABASE_URL only, .env gitignored. Minor non-blocking note: app/layout.tsx still has default Next.js scaffold metadata title ("Create Next App") — recommend switching to clientConfig.shopName in a later phase. All Phase 1 "Done when" criteria satisfied.

## Phase 2: Lead intake form — audited-pass
Notes: Public intake form on `/` (app/page.tsx + app/components/LeadIntakeForm.tsx, client component using `useActionState`). Validation in `lib/validation/leadForm.ts` (pure function, field-level errors, serviceType/urgency checked against allowed lists, requires phone or email). Server action `app/actions/submitLead.ts` validates then calls `lib/repo/leads.createLead` + `lib/repo/actionLog.logAction(lead.id, "submitted")` — no Prisma calls outside lib/repo/. Success renders inline SLA-expectation message in place of the form. Tests: `lib/validation/leadForm.test.ts` (7 cases) + `app/actions/submitLead.test.ts` (valid → Lead+"submitted" ActionLog; invalid → field errors, nothing created). Verified live via preview browser at 375px: form renders branded, invalid submit shows field errors and creates nothing, valid submit shows success message; POST confirmed 200 in network log.
Audit: 2026-07-07 — PASS. `npm test`: 4 files, 11/11 passed (leadForm.test.ts 7 cases, submitLead.test.ts 2 cases, plus Phase 1 repo tests). `npx tsc --noEmit` clean. `npx eslint .` clean. `npx prisma migrate status`: schema up to date. Booted `npx next dev -p 3402`; GET / returned 200 with live config-driven branding in markup (Summit HVAC Co., #1d4ed8, tagline, phone) — confirms no hardcoded branding reaches the client. Grepped app/ for hardcoded shop name/color/service-area strings: only match was "Aurora" used as fixture data inside submitLead.test.ts, not a violation. Grepped app/ for direct Prisma model calls outside lib/repo/: none in production code (submitLead.ts and LeadIntakeForm.tsx call only lib/repo functions); only matches are generated Prisma client files and the test file's own assertions. rawInput confirmed stored verbatim (plain String column, only .trim() applied, no AI rewriting exists yet in this phase). ActionLog "submitted" row confirmed written on every successful lead creation via integration test hitting the real DB. Invalid-input path confirmed to short-circuit before any DB call (validated in code and by test asserting zero Lead rows created). 375px usability verified structurally (fluid Tailwind classes, no fixed widths, max-w-md container, sm: breakpoint grid) rather than via visual screenshot — no browser/screenshot tool available in this session; recommend a manual visual pass before the live demo. Pre-existing non-blocking issue carried from Phase 1: app/layout.tsx still has default "Create Next App" title metadata (out of Phase 2 scope). All Phase 2 Done-when criteria and relevant RULES.md items (5, 6, 8, 9, 10, 13) satisfied.
Audit:

## Phase 3: AI triage pipeline — audited-pass
Notes: `lib/ai/emergencyKeywords.ts` — regex word-boundary keyword fast-path (gas, smoke, spark*, co, carbon monoxide, burning). `lib/ai/schema.ts` — zod schema for LLM triage output. `lib/ai/anthropicClient.ts` — calls claude-haiku via tool-use for structured output. `lib/ai/triage.ts` — `runTriage()`: keyword flag always computed; merges with LLM result (`isEmergency = keywordFlag || llm.isEmergency`); no API key → pending fallback (classification "pending", serviceNotes = raw input verbatim); malformed LLM output (zod parse fail) or thrown error → same pending fallback, keyword flag preserved. Wired into `app/actions/submitLead.ts` after lead+"submitted" log: runs triage, `lib/repo/triageResult.ts` stores result, logs "triaged" ActionLog; the triage/store/log sequence is wrapped in try/catch so any failure (including `createTriageResult` throwing) logs "triage_failed" instead of throwing past the point the Lead is already saved — lead intake always succeeds (rule 11). Eval set `lib/ai/evals/cases.ts` (12 cases incl. the vague 9pm gas-smell story from PLAN.md) + `evals.test.ts` — zero missed emergencies on gas/CO/smoke/spark/burning cases, zero false positives on routine cases, run via `npm test` (deterministic, no live API key needed since keyword fast-path guarantees the result independent of the LLM). Bugs caught by tests during build: "co" substring-matched "cold" (fixed with word boundaries), "sparks" keyword didn't match "sparking" (fixed with `\bspark\w*` stem match).
Audit: 2026-07-07 — first pass audited-fail: `app/actions/submitLead.ts` had no try/catch around the triage/store/log sequence — if `createTriageResult` threw (e.g. DB error), the whole server action would throw after the Lead was already committed, breaking rule 11 ("intake must never fail because triage failed") and leaving no ActionLog trace of the failure. Fixed: wrapped the sequence in try/catch, catch branch logs "triage_failed" ActionLog and still returns `success: true`. Added `app/actions/submitLead.triageFailure.test.ts` (mocks `createTriageResult` to throw) proving the lead is created, "submitted" + "triage_failed" logs exist, and `submitLead` never throws. Re-verified: `npx vitest run` 9 files/29 passed, `npx tsc --noEmit` clean, `npx eslint .` clean. All Phase 3 Done-when criteria and RULES.md items 6, 7, 9, 10, 11, 12, 14 confirmed. Non-blocking notes carried forward: (1) eval set currently only exercises the keyword fast-path since no `ANTHROPIC_API_KEY` is configured in this dev environment — rerun `lib/ai/evals/evals.test.ts` with a real key before any live demo per rule 15, to confirm LLM classification quality (the boolean emergency result can't regress since keyword flag is OR'd in, but classification text/SLA hint wording should be checked). (2) `createTriageResult` is a plain `create`, not an upsert — Phase 6's "regenerate triage" button will need to switch this to upsert; flagged for that phase, not a Phase 3 gap.

## Phase 4: Admin dashboard — audited-pass
Notes: Env-based login (`lib/auth.ts`: `verifyCredentials` against `ADMIN_USERNAME`/`ADMIN_PASSWORD`, session token via Web Crypto HMAC-SHA256 signed with `ADMIN_SESSION_SECRET` so it works in the Edge middleware runtime with no session store). `middleware.ts` protects `/admin/:path*` except `/admin/login`, redirecting unauthenticated requests to login. `app/admin/login/` (page + client `LoginForm` using `useActionState` + `actions.ts` server action) sets an httpOnly cookie on success; `app/admin/actions/logout.ts` clears it. `app/admin/layout.tsx` adds a shared header (shop name + logout) at 375px-safe flex layout. `app/admin/page.tsx` lists all leads via new `lib/repo/leads.getAllLeads()` (joins `triageResult`), with status and emergency-only filters via searchParams/GET form, and an EMERGENCY badge using `emergencyOverride ?? isEmergency`. `app/admin/leads/[id]/page.tsx` shows raw input vs AI summary side-by-side, full ActionLog timeline, `StatusControls` (client, calls new `app/admin/actions/updateStatus.ts` → `lib/repo/leads.updateLeadStatus` + logs `status_changed`), and `OverrideControls` (client, calls new `app/admin/actions/overrideEmergency.ts` → new `lib/repo/triageResult.setEmergencyOverride` + logs `emergency_override` set/cleared). Status options centralized in `lib/leadStatus.ts` (new/contacted/scheduled/closed) reused by validation-free UI and the action's guard. `.env.example` documents new `ADMIN_USERNAME`/`ADMIN_PASSWORD`/`ADMIN_SESSION_SECRET`. All DB access still routed through `lib/repo/`; all mutations log ActionLog rows (rule 9); no Prisma calls in components/actions.
Tests added: `lib/auth.test.ts` (credential + session token cases, incl. unconfigured-creds rejection), `lib/repo/leads.test.ts` (+updateLeadStatus, +getAllLeads includes triageResult), `lib/repo/triageResult.test.ts` (+setEmergencyOverride set/clear), `app/admin/actions/updateStatus.test.ts` (persists + logs, rejects invalid status), `app/admin/actions/overrideEmergency.test.ts` (set + clear, both logged in order). `next/cache` mocked in the two action test files since `revalidatePath` throws outside a real request context. `npm test`: 12 files / 40 passed. `npx tsc --noEmit` clean. `npx eslint .` clean.
Not verified live: a dev server from another session already holds this project directory's Next dev lock (PID noted in server output), so this session's `preview_start` couldn't boot its own instance without killing that session's process — skipped rather than risk another chat's in-progress work. Recommend a manual click-through (login → filter leads → open a lead → change status → toggle override → confirm timeline entries) once that other dev server is free, before /audit or a live demo.
Audit: 2026-07-07 — PASS. `npm test`: 12 files/40 passed. `npx tsc --noEmit` clean. `npx eslint .` clean (exit 0). `npx prisma migrate status`: schema up to date, no drift. Verified per-criterion: (1) lead list — `lib/repo/leads.ts getAllLeads()` joins triageResult, tested in `lib/repo/leads.test.ts`; `app/admin/page.tsx` renders EMERGENCY badge from `emergencyOverride ?? isEmergency` and status/emergency filters via searchParams, filter logic read directly and is correct (no dead filter). (2) status changes — `app/admin/actions/updateStatus.ts` validates against STATUS_OPTIONS, persists via `updateLeadStatus`, logs `status_changed` with detail=new status; `updateStatus.test.ts` proves persistence + log against a real DB, and rejection of invalid status. (3) detail view — `app/admin/leads/[id]/page.tsx` shows rawInput (verbatim, never mutated — rule 10 satisfied) and AI summary side-by-side (grid sm:grid-cols-2), full ActionLog timeline via getActionLogsForLead. (4) override — `app/admin/actions/overrideEmergency.ts` -> `lib/repo/triageResult.setEmergencyOverride` (nullable Boolean column, set/clear both tested) + logs `emergency_override` with set_emergency/set_not_emergency/cleared detail; `overrideEmergency.test.ts` proves set+clear+log-order against real DB. (5) 375px — structural read only (no live browser this session either, same limitation as prior phases): no fixed pixel widths anywhere in app/admin, filter form/list/controls use flex flex-wrap, raw/summary panel uses grid sm:grid-cols-2 (collapses to 1 column below sm). Minor non-blocking note: app/admin/layout.tsx header row lacks flex-wrap — low risk with current shopName length but worth a live check before demo. middleware.ts correctly gates /admin/* (allows /admin/login through, redirects on missing/invalid HMAC session cookie) — functionally sufficient for single-admin v1 per CLAUDE.md. Rule 6: grepped app/admin for prisma. calls — none in production code, only in test files (consistent with Phase 1/2 precedent). Rule 8: grepped app/admin for hardcoded shop name/color/service strings — none found; all branding reads clientConfig. Rule 9: both status change and override write ActionLog rows with correct detail; login/logout correctly treated as session events, not lead mutations, so no ActionLog expected there — not a gap. No real bugs found (no stale logic, no filter that fails to filter, no status update that fails to persist). All Phase 4 Done-when criteria and RULES.md items 1, 5, 6, 8, 9, 10, 13, 14 satisfied.

## Phase 5: Follow-up drafts — audited-pass
Notes: `lib/followUpDraftType.ts` — 4 draft types (quote_followup, booking_confirmation, more_details, maintenance_reminder) × 2 variants (email/sms). `lib/ai/followUpDraftSchema.ts` — zod schema (`sms` capped at 300 chars, forcing schema-validation fallback if the LLM overruns). `lib/ai/anthropicFollowUpClient.ts` + `lib/ai/followUpDraftPrompts.ts` — tool-use call to claude-haiku per draft type, with lead + triage context. `lib/ai/followUpDraft.ts` — `runFollowUpDraft()`: no API key, LLM throw, or schema-validation failure (incl. oversized SMS) all fall back to hardcoded per-type templates built from lead/shop fields — same graceful-degradation pattern as triage (rule 11/CLAUDE.md AI-unavailable requirement), never blocks. `lib/repo/followUpDraft.ts` — create/list/update, all DB access routed through repo per rule 6. `app/admin/leads/[id]/actions/generateDraft.ts` — generates both variants for a type, creates 2 FollowUpDraft rows, logs 2 `draft_generated` ActionLog rows (`type:variant` detail) — rule 9. `app/admin/leads/[id]/actions/updateDraft.ts` — persists an edited draft, logs `draft_edited` (detail=draftId). `app/admin/leads/[id]/FollowUpDrafts.tsx` (client) — per-type Generate buttons, editable textarea per draft with Save/Copy (clipboard), and an "Open in email" mailto link for email variants when lead.email exists; wired into `app/admin/leads/[id]/page.tsx` above the timeline. No hardcoded shop name — templates and mailto subject/signature pull from `client.config.ts`.
Tests: `lib/ai/followUpDraft.test.ts` (all 4 types produce a template with no key; LLM success path; schema-fail fallback; oversized-SMS fallback; LLM-throw fallback), `lib/repo/followUpDraft.test.ts` (create+read, update), `app/admin/leads/[id]/actions/generateDraft.test.ts` (creates email+sms pair, both `draft_generated` logs against real DB), `app/admin/leads/[id]/actions/updateDraft.test.ts` (persists edit + `draft_edited` log). `npx vitest run`: 16 files/49 passed. `npx tsc --noEmit` clean. `npx eslint .` clean.
Not verified live: same constraint as Phase 4 — another session's `next dev` already holds this project's dev-server lock (PID logged in `.next/dev/logs`), and this session's `preview_start` refused to start a second instance rather than kill that session's process. `generateDraft`/`updateDraftContent` integration tests exercise the real flow against the actual SQLite DB (Lead → FollowUpDraft rows → ActionLog rows), which substitutes for a browser click-through of DB/logging correctness, but the UI itself (button states, textarea editing, copy-to-clipboard, mailto link, 375px layout) has not been visually confirmed. Recommend a manual click-through once that other dev server is free, before /audit or a live demo.
Audit: 2026-07-07 — PASS. `npx vitest run`: 16 files/49 passed. `npx tsc --noEmit` clean. `npx eslint .` clean (exit 0). `npx prisma migrate status`: schema up to date, no drift, 1 migration. Verified per-criterion: (1) 8 combos — `lib/followUpDraftType.ts` defines 4 types x 2 variants; `runFollowUpDraft()` in `lib/ai/followUpDraft.ts` covers all 4 types in its `TEMPLATES` fallback map (tested by a loop over all `DRAFT_TYPES` in `followUpDraft.test.ts` asserting non-empty email+sms for each); `generateDraft.ts` creates both variants per call, proven end-to-end against the real DB in `generateDraft.test.ts`. (2) editable/copyable — `FollowUpDrafts.tsx` textarea + Save (calls `updateDraftContent`) + Copy (clipboard) + mailto for email variants when `lead.email` exists; edit persistence proven against real DB in `updateDraft.test.ts`. (3) every generation logged — `generateDraft.ts` writes 2 `draft_generated` ActionLog rows (`type:email`/`type:sms`) per call, `updateDraft.ts` writes 1 `draft_edited` row per edit; both proven against real DB with correct `detail` values (rule 9). (4) SMS under 300 chars, casual tone — enforced by `followUpDraftOutputSchema` (`sms` `.max(300)`, oversized LLM output forces template fallback, tested with a 301-char case); stress-tested templates with extreme-length names manually (worst case 215 chars, well under 300); tone check: SMS templates use contractions/exclamations/first-name address (casual), email templates use "Hi [name]," / "Thanks, [shop]" (semi-formal) — matches plan's split, and the LLM prompt in `anthropicFollowUpClient.ts` explicitly instructs the same split. Rule 6: grepped `app/` for `prisma.` calls — only test files and generated client code, no violations in `generateDraft.ts`/`updateDraft.ts`. Rule 7: `runFollowUpDraft` is the sole AI entry point, zod `safeParse`, explicit fallback branches for missing key/LLM throw/schema failure, all covered by tests. Rule 8: grepped `app/admin/leads`, `lib/ai/followUpDraft*.ts`, `lib/followUpDraftType.ts` for hardcoded shop name/color — only hit is test fixture data in `followUpDraft.test.ts` (not production code, same pattern accepted in earlier phase audits); production code sources `shopName`/`shopPhone` from `client.config.ts`. Rule 9: confirmed above. Rule 10: grepped for `rawInput` in draft-related AI/action code — no references; drafts never touch `Lead.rawInput`. Rule 13: structural-only assessment again this session (no live browser — dev server lock still held by another session, same limitation Phase 5's own notes already flagged); `FollowUpDrafts.tsx` uses `flex flex-wrap` for button rows, `grid gap-3 sm:grid-cols-2` for the draft pair (collapses to 1 column below `sm`, correct for 375px), `textarea` is `w-full`, no fixed-pixel widths found via grep — structurally sound, recommend the still-outstanding manual click-through before a live demo. Rule 14: `.env.example` has all blank values, `.env*` gitignored, no secrets committed. **Gap found (pre-existing, not introduced by Phase 5, still live):** `.gitignore`'s `/dev.db` pattern is root-anchored and does not match `prisma/dev.db`, where the real SQLite file lives (confirmed via `git check-ignore -v prisma/dev.db` -> no match). File is currently untracked and never committed, so no active leak yet, but it is one `git add -A` away from being committed with real lead data — this predates Phase 5 (present since Phase 1) and slipped through Phases 1-4 audits too. Fix: change to `**/dev.db` before Phase 6 seeds ~20 leads into that file. Non-blocking for this phase's Done-when criteria (which don't govern it) but should be closed before the live demo. All 4 Phase 5 Done-when criteria and RULES.md items 5, 6, 7, 8, 9, 10, 13, 14 confirmed; verdict audited-pass.

## Phase 6: Demo polish — audited-pass
Notes: `prisma/seed.ts` rewritten to insert 20 realistic leads (varied service types/locations/urgency/status, several emergency cases incl. the vague 9pm gas-smell missed-lead story from PLAN.md, a few with follow-up drafts generated) — each goes through the same submitted→triaged→(status_changed)→(draft_generated) ActionLog sequence as the real app (rule 9), run via `npx prisma db seed` against the real SQLite DB (verified: "Seeded 20 leads." with no errors, all triage/draft calls degrading gracefully via existing keyword/template fallback since no ANTHROPIC_API_KEY is set in dev — deterministic, matches Phase 3/5 pattern). Static FAQ page (`app/faq/page.tsx`) renders `clientConfig.faq` (expanded to 4 entries in `client.config.ts` — data only, no hardcoded copy in the page, rule 8); linked from `/` intake page. Empty states: `app/admin/page.tsx` now distinguishes a genuinely empty DB ("No leads yet — submitted leads will show up here.") from a filter that matches nothing ("No leads match this filter."); `FollowUpDrafts.tsx` shows "No drafts generated yet." before any type has been generated. "Regenerate triage" button: new `lib/repo/triageResult.upsertTriageResult()` (Prisma `upsert`, closing the gap flagged in the Phase 3/5 audits where `createTriageResult` was a plain `create`), new `app/admin/leads/[id]/actions/regenerateTriage.ts` server action re-runs `runTriage()` against the lead's stored raw input, upserts the result, and logs a `triage_regenerated` ActionLog row with the new classification as detail (rule 9); wired into the lead detail page via `RegenerateTriageButton.tsx` next to the AI summary heading.
Tests added: `lib/repo/triageResult.test.ts` (+2 cases: upsert-creates, upsert-replaces-existing), `app/admin/leads/[id]/actions/regenerateTriage.test.ts` (overwrites triage + logs `triage_regenerated`; throws on missing lead). `npx vitest run`: 17 files/53 passed. `npx tsc --noEmit` clean. `npx eslint .` clean (exit 0). `npx prisma migrate status`: schema up to date, no drift.
Not verified live: same constraint as Phases 4-5 — another session's `next dev` holds this project directory's dev-server lock; this session's `preview_start` refused a second instance (directory-scoped lock, not port-scoped, so a different port doesn't help) rather than kill that session's process. Recommend a manual click-through (empty-DB state before seeding, seeded dashboard after, FAQ page, regenerate-triage button live) once that other dev server is free, before /audit or a live demo.
Audit: 2026-07-07 — PASS. Live dev server already running on port 3401 (this session did not need to boot its own, resolving the Phase 4-6 dev-server-lock limitation) — used it for live verification alongside the test suite. `npx vitest run`: 17 files/53 passed (includes `lib/ai/evals/evals.test.ts` run separately too: 2/2 passed, zero missed emergencies on gas/CO/smoke/sparks/burning cases, rule 15 satisfied). `npx tsc --noEmit` clean. `npx eslint .` clean (exit 0). `npx prisma migrate status`: schema up to date, no drift. Verified per-criterion: (1) one seed command, full dashboard — `prisma/seed.ts` read in full: 20 leads with varied service types/locations/statuses (new/contacted/scheduled/closed), multiple emergency cases (gas smell, CO alarm, smoke, sparking, burning smell), and the exact vague-9pm-gas-smell story from PLAN.md ("Priya Nair" — "it's 9pm and i just noticed a faint gas smell near the furnace, not sure if its urgent", urgency: routine, status: new — correctly relies on the keyword fast-path to flag it despite the customer downplaying it); ran `npx prisma db seed` live against the real SQLite DB this session — output "Seeded 20 leads." with no errors; confirmed live via curl that `/admin` and `/faq` continued to serve correctly post-seed. Every seed lead goes through submitted -> triaged (or triage_failed) -> optional status_changed -> optional draft_generated x2, each writing an ActionLog row via `logAction`, matching rule 9; read the loop in `seedLeads` directly to confirm no lead skips the submitted/triaged log pair. (2) FAQ from config only — `app/faq/page.tsx` read in full: renders `clientConfig.shopName`, `clientConfig.brandColor`, `clientConfig.serviceArea`, `clientConfig.phone`, and maps `clientConfig.faq` (4 entries, expanded in `client.config.ts`) into a `<dl>` — zero hardcoded copy in the page; grepped `app/faq` for shop-name/color/service-area literals — no matches; curled the live page and confirmed all 4 questions plus "Summit HVAC" render server-side from config; `app/page.tsx` links to `/faq`. (3) empty states — `app/admin/page.tsx` read in full: two distinct `<li>` branches, `filtered.length === 0 && leads.length === 0` -> "No leads yet — submitted leads will show up here.", vs `filtered.length === 0 && leads.length > 0` -> "No leads match this filter." — correctly distinct, not the same string; `FollowUpDrafts.tsx` shows "No drafts generated yet." when `drafts.length === 0`, independent per-type generate buttons still render so an operator can act immediately. (4) regenerate triage works live and logs — `app/admin/leads/[id]/actions/regenerateTriage.ts` read in full: pulls `lead.rawInput` via `getLeadById` (rule 6, no direct Prisma), re-runs `runTriage()` (still the sole zod-`safeParse`-validated AI entry point, rule 7 — grepped `lib/ai/triage.ts` to confirm `safeParse` still gates the LLM output), calls `upsertTriageResult` (not the old plain `create`, closing the Phase 3/5-flagged gap), logs `triage_regenerated` with the new classification as detail (rule 9), never writes to `lead.rawInput` (grepped for rawInput writes in leads.ts/regenerateTriage.ts — none, rule 10 intact). `lib/repo/triageResult.ts`'s `upsertTriageResult` uses a real Prisma `upsert` (create-or-update on the unique `leadId`), directly fixing the crash-on-second-call bug the plain `create` had. Test coverage: `lib/repo/triageResult.test.ts`'s "upsert replaces an existing triage result for the same lead" case proves the update branch works end-to-end against a real DB (create a pending TriageResult, then upsert over it, assert the new values won) — the critical regression test for the original bug. `app/admin/leads/[id]/actions/regenerateTriage.test.ts` proves the action end-to-end (creates lead, calls `regenerateTriage`, asserts `isEmergency` true and a `triage_regenerated` ActionLog row exists) plus a not-found-lead throw case. Minor non-blocking gap: no single test calls the `regenerateTriage` action itself twice on the same lead to prove the action's second-call path specifically (as opposed to the repo function in isolation) — low risk since the action is a thin wrapper with no branching logic of its own, but worth a live click-of-the-button-twice check before demo. Live-verified via curl: `/admin` correctly redirects unauthenticated requests to `/admin/login` (middleware gate intact) even with 20 leads now in the DB; login page renders branded ("Summit HVAC Co. — Admin login") from config, not hardcoded. Did not have admin credentials available in this session (`.env` read is blocked by a standing deny rule) so could not click through the authenticated dashboard/detail page/regenerate-button flow pixel-by-pixel; relied on structural code read + passing DB-level integration tests for that portion, consistent with the precedent in Phases 4-5's audits. Rule 6: grepped `regenerateTriage.ts`, `app/faq/page.tsx`, `app/admin/page.tsx`, `FollowUpDrafts.tsx`, `prisma/seed.ts` for `prisma.` calls — none, all routed through `lib/repo/`. Rule 8: grepped `app/` for hardcoded shop-name/color/service-area strings — only matches are test fixture files (accepted pattern per Phase 1-5 precedent), zero in production code including the new `app/faq/`. Rule 13: structural check only (same live-browser limitation noted above for the authenticated portion) — `app/admin/page.tsx` filter form uses `flex flex-wrap`, lead list has no fixed widths; `FollowUpDrafts.tsx` uses `grid gap-3 sm:grid-cols-2` (collapses to 1 column below 375px-class breakpoints) and `w-full` textareas; `app/faq/page.tsx` uses `max-w-2xl` fluid container. Gap closed since last audit: `.gitignore`'s `dev.db` pattern (flagged non-blocking in the Phase 5 audit) is now `**/dev.db` — confirmed via `git check-ignore -v prisma/dev.db` matching; `prisma/dev.db` is untracked and correctly ignored, no risk of committing seeded lead data. All 4 Phase 6 Done-when criteria and RULES.md items 6, 7, 8, 9, 10, 13, 15 satisfied; verdict audited-pass.

## Decision log
- 2026-07-05: Plan approved. Portfolio demo first, modular per-client redeploy model. Knowledge Assistant chatbot cut from v1 (static FAQ instead).
- 2026-07-05: Stack: Next.js + TS, Prisma (SQLite→Postgres), Tailwind + shadcn/ui, Anthropic API, Vercel.
- 2026-07-07: Phase 1 deps added: `prisma`, `@prisma/client` (ORM per plan), `vitest` (test runner for repo-layer TDD), `@prisma/adapter-better-sqlite3` (Prisma 7 requires a driver adapter for SQLite, no more plain `url`), `tsx` (run TS seed script).
- 2026-07-07: Phase 3 deps added: `zod` (structured/validated LLM output per RULES 7), `@anthropic-ai/sdk` (Anthropic API calls, plan-specified stack).
